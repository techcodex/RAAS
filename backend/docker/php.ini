; RAAS backend container overrides.
; Uploads are gated by the app (config/raas.php, default 50 MB); keep the PHP
; limits comfortably above that so validation — not PHP — rejects large files.
upload_max_filesize = 64M
post_max_size = 68M
memory_limit = 256M
max_execution_time = 120
